{{ form_start(form) }}
<div class="row">
    <div class="col-12">
        {{ form_errors(form) }}
    </div>
</div>
<div class="row">
    <div class="col-12">
        {{ form_label(form.title, 'Title', {'label_attr' : {'class' : 'form-label' }}) }}
        {{ form_errors(form.title) }}
        {{ form_widget(form.title, {'attr' : {'class' : 'form-control'}}) }}
        {{ form_help(form.title) }}
    </div>
</div>
<div class="row">
    <div class="col-12">
        {{ form_label(form.slug, 'slug', {'label_attr' : {'class' : 'form-label' }}) }}
        {{ form_errors(form.slug) }}
        {{ form_widget(form.slug, {'attr' : {'class' : 'form-control'}}) }}
        {{ form_help(form.slug) }}
    </div>
</div>
<div class="row">
    <div class="col-12">
        {{ form_label(form.body, 'Body', {'label_attr' : {'class' : 'form-label' }}) }}
        {{ form_errors(form.body) }}
        {{ form_widget(form.body, {'attr' : {'class' : 'form-control'}}) }}
        {{ form_help(form.body) }}
    </div>
</div>
<div class="row">
    <div class="col-12">
        {{ form_label(form.author, 'Author', {'label_attr' : {'class' : 'form-label' }}) }}
        {{ form_errors(form.author) }}
        {{ form_widget(form.author, {'attr' : {'class' : 'form-control'}}) }}
        {{ form_help(form.author) }}
    </div>
</div>
<div class="row mt-2">
    <div class="col-12">
        <button class="btn btn-primary btn-block">{{ button_label|default('Save') }}</button>
    </div>
</div>


{{ form_end(form) }}
