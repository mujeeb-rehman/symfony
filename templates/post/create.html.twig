{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-8">
            <h1>Create new Post</h1>

            {{ include('post/_form.html.twig') }}

            <a href="{{ path('post_index') }}" class="btn btn-dark mt-2">back to list</a>
        </div>
    </div>

{% endblock %}
